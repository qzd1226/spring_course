ch01-hello-spring: use ioc, use spring to creat object
steps：
1、create maven project
2、add maven dependency
   spring dependency, version: 5.2.5
   junit dependency
3、create class (interface and realization class)
    it's a normal class
4、create the config file that spring needs
    Declare information about classes that spring creates and manages
5、test the object created by spring
